class Ride {
    constructor(id, driverId, childId, status) {
        this.id = id;
        this.driverId = driverId;
        this.childId = childId;
        this.status = status;
    }
}

export default Ride;